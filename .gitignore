node_modules/
dist/
*.log
.DS_Store
Thumbs.db

# Vendored third-party builds fetched by spike/fetch-vendor.sh — reproducible
# from pinned URLs + checksums in spike/README.md, not committed (large text).
/vendor/
/spike/dist/

# AI agent governance rule files — kept locally, not published (user request 2026-08-05)
/AGENTS.md
/CLAUDE.md
/.antigravity_rules.md

# Local test fixtures — real-world files used for manual testing only,
# never published (e.g. docs/test.pdf, a real unrelated document used to
# exercise Phase 2 PDF parsing; user request 2026-08-05)
docs/test.pdf

# Synthetic test PDFs generated by scripts/make-test-pdf.mjs — reproducible,
# not committed (Phase 2 Source Engine manual/browser testing).
/fixtures/
