# Antigravity Rules

All project rules live in `AGENTS.md` at the repo root. Read it completely and follow it
before any task. If its "Project Facts" section is empty, run the Bootstrap procedure
defined there first. Do not add rules to this file.
